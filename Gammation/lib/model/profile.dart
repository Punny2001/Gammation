class Profile {
  String username;
  String password;
  String firstname;
  String lastname;
  String address;
  String province;
  String country;

  Profile(this.username, this.password, this.firstname, this.lastname,
      this.address, this.province, this.country);
}
